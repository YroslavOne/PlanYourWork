import { Context } from "../../../Context";
import { useContext } from "react";


function SettingsTimer(){
const {settingsTimer, setScreensTimer, setSettingsTimer } = useContext()


return(
    <div>
        hi
    </div>

)
}
export default SettingsTimer;